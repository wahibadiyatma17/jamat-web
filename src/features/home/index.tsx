import AppLayout from '@/components/layout';
import { FC } from 'react';

const Home: FC = () => {
  return (
    <AppLayout>
      <div className="h-screen bg-red-200"></div>
    </AppLayout>
  );
};

export default Home;
