import AppLayout from '@/components/layout';
import { FC } from 'react';

const StoreRegistration: FC = () => {
  return (
    <AppLayout>
      <div className="flex flex-col gap-[80px]"></div>
    </AppLayout>
  );
};

export default StoreRegistration;
